<van-tabbar active="{{ selected }}" bind:change="onChange" active-color="{{activeColor}}" inactive-color="{{mainColor}}"
  custom-class="my-tabbar">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
    }}</van-tabbar-item>
</van-tabbar>